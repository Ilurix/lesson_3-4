git config
git init
git status

git add (filename)
git add .

git commit -m "message"
git commit -a -m "message"
git commit --amend

git log
git log --pretty
git log -p -2

git reset (filename)
git reset .

git checkout id
git checkout master